# ClearPuff screenshots

Store the approved, release-specific screenshots here. Do not replace real store captures with website mockups.

Recommended naming:

```text
android-phone-en-01-home.png
android-phone-en-02-progress.png
ios-6.7-en-01-home.png
```

Update `app.json` and set `releaseReadiness.screenshots` to `true` only after the referenced files exist and match the shipping build.
